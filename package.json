{
  "name": "ai-usage-widget",
  "version": "1.4.1",
  "private": true,
  "description": "Cross-platform desktop widget for local AI usage limits.",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "check:public": "node scripts/public-check.js",
    "test:macos": "npm test --prefix apps/macos",
    "test:windows": "npm test --prefix apps/windows",
    "test": "npm run check:public && npm run test:macos && npm run test:windows"
  }
}
